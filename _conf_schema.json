{
    "trigger_settings": {
        "description": "触发设置",
        "type": "object",
        "hint": "控制机器人何时允许调用文件工具",
        "items": {
            "require_at_in_group": {
                "description": "群聊需要@/引用机器人",
                "type": "bool",
                "hint": "开启后，群聊中仅在@/引用机器人时模型才会获得文件操作能力。这能有效防止模型在群聊中误触发工具并节省Token。",
                "default": true
            },
            "enable_features_in_group": {
                "description": "群聊启用插件功能",
                "type": "bool",
                "hint": "关闭后，本插件在群聊中完全不生效（LLM工具、文件拦截与插件命令均禁用），仅私聊可用。默认关闭以降低群聊误触发风险。",
                "default": false
            },
            "auto_block_execution_tools": {
                "description": "自动屏蔽 shell/python 工具",
                "type": "bool",
                "hint": "开启后，当插件功能生效时会自动隐藏 astrbot_execute_shell / astrbot_execute_python / astrbot_execute_ipython，避免模型误用系统执行工具导致文件定位跑偏。",
                "default": true
            },
            "reply_to_user": {
                "description": "发送文件时@用户",
                "type": "bool",
                "hint": "发送生成的文件时，是否@用户。",
                "default": true
            }
        }
    },
    "permission_settings": {
        "description": "权限管理",
        "type": "object",
        "hint": "配置可以使用插件的用户",
        "items": {
            "whitelist_users": {
                "description": "用户白名单",
                "type": "list",
                "hint": "允许使用插件的用户ID，每行一个。留空则仅管理员可用。",
                "default": []
            }
        }
    },
    "feature_settings": {
        "description": "功能开关",
        "type": "object",
        "hint": "启用或禁用特定功能",
        "items": {
            "enable_office_files": {
                "description": "启用 Office 文件生成",
                "type": "bool",
                "hint": "是否允许生成 Word/Excel/PPT 文件。",
                "default": true
            },
            "enable_pdf_conversion": {
                "description": "启用 PDF 转换",
                "type": "bool",
                "hint": "是否允许 Office⇄PDF 互转功能。需要额外安装依赖：LibreOffice (Office→PDF)、pdf2docx (PDF→Word)、tabula-py (PDF→Excel)。",
                "default": true
            }
        }
    },
    "file_settings": {
        "description": "文件限制",
        "type": "object",
        "hint": "控制文件操作的限制",
        "items": {
            "max_file_size_mb": {
                "description": "最大文件大小(MB)",
                "type": "int",
                "hint": "允许读取/发送的最大文件大小，单位为 MB。设置过大可能导致内存不足或发送失败。",
                "default": 20
            },
            "enable_docx_image_review": {
                "description": "启用 Word 图片理解",
                "type": "bool",
                "hint": "读取 .docx 时，是否把文档里的嵌入图片注入模型上下文。关闭后按纯文本读取，不再输出 [插图N] 占位。",
                "default": true
            },
            "max_inline_docx_image_mb": {
                "description": "Word图片注入大小上限(MB)",
                "type": "int",
                "hint": "读取 .docx 时，单张嵌入图片超过该大小就不注入多模态上下文，只在文本结果里保留说明。默认 2 MB。",
                "default": 2
            },
            "max_inline_docx_image_count": {
                "description": "Word图片最多注入张数",
                "type": "int",
                "hint": "读取 .docx 时，最多向多模态上下文注入多少张嵌入图片。超出的图片只在文本结果里保留说明。默认 3 张。",
                "default": 3
            },
            "auto_delete_files": {
                "description": "发送后自动删除文件",
                "type": "bool",
                "hint": "启用后，生成的文件会在发送完成后自动删除，不会占用磁盘空间。关闭则持久化存储在 data/plugin_data/astrbot_plugin_office_assistant/ 目录。",
                "default": true
            },
            "message_buffer_seconds": {
                "description": "文件合并等待时间(秒)",
                "type": "float",
                "hint": "上传文件后，系统会先等几秒，看看你是不是还要继续发别的文件。这个时间内发来的文件会被当成同一批处理。",
                "default": 4
            },
            "recent_text_ttl_seconds": {
                "description": "旧流程文本缓存时间(秒)",
                "type": "int",
                "hint": "兼容旧流程用，当前 `/doc` 模式下基本不会用到，一般不用改。",
                "default": 20
            },
            "upload_session_ttl_seconds": {
                "description": "上传文件保留时间(秒)",
                "type": "int",
                "hint": "上传完成后，文件会在当前会话里保留这段时间，供 `/doc list`、`/doc use`、`/doc clear` 使用。超过时间后会自动失效。",
                "default": 600
            }
        }
    },
    "path_settings": {
        "description": "路径访问设置",
        "type": "object",
        "hint": "控制 read_file / PDF 转换是否允许访问工作区外文件",
        "items": {
            "allow_external_input_files": {
                "description": "允许外部绝对路径",
                "type": "bool",
                "hint": "开启后，read_file、convert_to_pdf、convert_from_pdf 可读取工作区外绝对路径文件。默认关闭更安全。delete_file 仍仅允许工作区内文件。",
                "default": false
            }
        }
    },
    "preview_settings": {
        "description": "预览图设置",
        "type": "object",
        "hint": "控制文件预览图生成",
        "items": {
            "enable": {
                "description": "启用预览图",
                "type": "bool",
                "hint": "发送 Office/PDF 文件时是否同时发送第一页预览图",
                "default": true
            },
            "dpi": {
                "description": "预览图分辨率",
                "type": "int",
                "hint": "预览图的 DPI 分辨率，越高越清晰但文件越大。推荐 100-200。",
                "default": 150
            }
        }
    },
    "word_style_settings": {
        "description": "Word 默认样式",
        "type": "object",
        "hint": "控制 Word 文档默认字体。模型没有显式传 document_style 字体字段时，会使用这里的配置。",
        "items": {
            "default_font_name": {
                "description": "正文默认字体",
                "type": "string",
                "hint": "例如 Arial、Microsoft YaHei、Source Han Sans SC。留空则继续使用内置默认值。",
                "default": ""
            },
            "default_heading_font_name": {
                "description": "标题默认字体",
                "type": "string",
                "hint": "留空时跟随正文默认字体。",
                "default": ""
            },
            "default_table_font_name": {
                "description": "表格默认字体",
                "type": "string",
                "hint": "留空时跟随正文默认字体。",
                "default": ""
            },
            "default_code_font_name": {
                "description": "代码默认字体",
                "type": "string",
                "hint": "留空时继续使用内置默认值。",
                "default": ""
            }
        }
    },
    "debug_settings": {
        "description": "调试设置",
        "type": "object",
        "hint": "用于插件报错的转发测试",
        "items": {
            "debug_error_hook_target_session": {
                "description": "报错转发目标会话",
                "type": "string",
                "hint": "格式: <platform.id>:<MessageType>:<target_id>。群聊示例: default:GroupMessage:123456789；私聊示例: default:FriendMessage:123456789。注意前缀是 platform.id（如 default），不是 platform.type（如 aiocqhttp）。留空则回落到当前会话。",
                "default": ""
            }
        }
    }
}
