name: Sync issue labels

on:
  push:
    paths:
      - ".github/workflows/label-sync.yml"
      - ".github/workflows/issue-auto-label.yml"
      - ".github/workflows/pr-auto-label.yml"
      - ".github/labeler/config.json"
  workflow_dispatch:

jobs:
  sync-labels:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync labels from shared config
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require("node:fs");
            const config = JSON.parse(
              fs.readFileSync(".github/labeler/config.json", "utf8"),
            );
            const labels = config.labels || [];
            const legacyLabelRenames = new Map([
              ["size:xs", "size:XS"],
              ["size:s", "size:S"],
              ["size:m", "size:M"],
              ["size:l", "size:L"],
              ["size:xl", "size:XL"],
            ]);

            for (const [oldName, newName] of legacyLabelRenames.entries()) {
              const target = labels.find((label) => label.name === newName);
              if (!target) {
                continue;
              }
              try {
                await github.rest.issues.getLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  name: oldName,
                });

                await github.rest.issues.updateLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  name: oldName,
                  new_name: newName,
                  color: target.color,
                  description: target.description,
                });
              } catch (error) {
                if (error.status !== 404) {
                  throw error;
                }
              }
            }

            for (const label of labels) {
              try {
                await github.rest.issues.getLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  name: label.name,
                });

                await github.rest.issues.updateLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  name: label.name,
                  new_name: label.name,
                  color: label.color,
                  description: label.description,
                });
              } catch (error) {
                if (error.status === 404) {
                  await github.rest.issues.createLabel({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    name: label.name,
                    color: label.color,
                    description: label.description,
                  });
                } else {
                  throw error;
                }
              }
            }
