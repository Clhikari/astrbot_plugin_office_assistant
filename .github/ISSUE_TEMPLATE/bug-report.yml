name: "🐛 Report Bug / 报告 Bug"
description: Submit a reproducible bug report for astrbot_plugin_office_assistant.
title: "[Bug] "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting this issue.
        Please provide clear steps and complete logs so we can locate the root cause quickly.

  - type: textarea
    id: actual
    attributes:
      label: What happened / 发生了什么？
      description: Describe what happened and why it is wrong.
      placeholder: A clear and specific description of the bug.
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: What did you expect / 预期行为
      description: Describe the expected behavior.
      placeholder: What should happen instead?
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: Reproduce steps / 复现步骤
      description: List exact steps to reproduce this issue.
      placeholder: |
        1. ...
        2. ...
        3. ...
    validations:
      required: true

  - type: textarea
    id: env
    attributes:
      label: Environment / 环境信息
      description: Include plugin version, AstrBot version, adapter, deployment method, and model provider.
      placeholder: |
        Plugin: x.y.z
        AstrBot: x.y.z
        Adapter: aiocqhttp
        Deployment: Windows / Docker / Linux
        Provider: OpenAI-compatible / Gemini / ...
    validations:
      required: true

  - type: dropdown
    id: os
    attributes:
      label: OS
      description: Which OS are you using?
      multiple: false
      options:
        - Windows
        - macOS
        - Linux
        - Other
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: Logs / 日志
      description: Paste complete DEBUG logs and screenshots if available.
      render: shell
    validations:
      required: true

  - type: checkboxes
    id: willing-pr
    attributes:
      label: Are you willing to submit a PR? / 你愿意提交 PR 吗？
      description: This is optional, but contributions are welcome.
      options:
        - label: Yes, I can submit a PR for this issue.

  - type: checkboxes
    id: checks
    attributes:
      label: Checks
      options:
        - label: I searched existing issues and did not find a duplicate.
          required: true
        - label: I can reproduce this issue consistently.
          required: true

  - type: checkboxes
    id: coc
    attributes:
      label: Code of Conduct
      options:
        - label: I agree to follow the repository Code of Conduct.
          required: true
