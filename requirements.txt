# Office 文档处理（必需）
python-docx
openpyxl
python-pptx

# PDF 文本提取（推荐）
pdfplumber

# PDF→Word 转换
pdf2docx

# PDF→Excel 转换（tabula/pdfplumber 路径均依赖 pandas）
pandas

# PDF/Office 预览图生成
pymupdf

# PDF→Excel 高精度表格提取（可选，需 Java）
tabula-py

# 旧格式 .xls 读取（跨平台）
xlrd

# Office→PDF 转换及旧格式读取（仅 Windows，需要 Microsoft Office）
docx2pdf; sys_platform == 'win32'
pywin32; sys_platform == 'win32'
